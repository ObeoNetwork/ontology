= ontology-model

== Goal

This project is the entry point for meta-model used for Ontology.

== Dependencies

It depends on `org.obeonetwork.dsl` meta-models :

* org.obeonetwork.dsl.entity
* org.obeonetwork.dsl.environment
* org.obeonetwork.dsl.technicalid

== Development

This EMF version used to generate the code is

